@model List<Sale>
    <div class="card">
        <div class="card-body">
            <div class="row">
                <div class="col mt-0">
                    <h5 class="card-title">Tolam Kazanç</h5>
                </div>

                <div class="col-auto">
                    <div class="stat text-primary">
                        <i class="align-middle">TL</i>
                    </div>
                </div>
            </div>
            <h1 class="mt-1 mb-3">@Model.Sum(x=>x.Price)</h1>
            <div class="mb-0">
                @*<span class="text-success"> <i class="mdi mdi-arrow-bottom-right"></i> 6.65% </span>
                <span class="text-muted">Since last week</span>*@
            </div>
        </div>
    </div>